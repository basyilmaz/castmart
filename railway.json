{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "numReplicas": 1,
        "startCommand": "/bin/sh -c 'php artisan config:clear && php artisan storage:link --force 2>/dev/null || true && php artisan migrate --force && php -S 0.0.0.0:${PORT} -t public public/router.php'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
    }
}